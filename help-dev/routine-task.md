```js
&НаСервере
Процедура СоздатьРегламентноеЗаданиеНаСервере()
	
	Отбор = Новый Структура;
	Отбор.Вставить("Ключ", "тснРасчетАналитическихПризнаковПартнеров");    
	Если ОбщегоНазначения.РазделениеВключено() Тогда    
		Отбор.Вставить("ОбластьДанных", ПараметрыСеанса.ОбластьДанныхЗначение);      
	КонецЕсли;  

	НайденныеРеглЗадания = РегламентныеЗаданияСервер.НайтиЗадания(Отбор);  

	Если НайденныеРеглЗадания.Количество() = 0 Тогда 
		
		ПараметрыЗапуска = Новый Массив();
		ПараметрыЗапуска.Добавить("тснРасчетАналитическихПризнаковПартнеров");
		ПараметрыЗапуска.Добавить(Неопределено);

		ПараметрыЗадания = Новый Структура();
		ПараметрыЗадания.Вставить("Метаданные", Метаданные.РегламентныеЗадания.ЗапускДополнительныхОбработок);
		ПараметрыЗадания.Вставить("Наименование", "Расчет аналитических признаков");
		ПараметрыЗадания.Вставить("Ключ", "тснРасчетАналитическихПризнаковПартнеров");
		ПараметрыЗадания.Вставить("Использование", Истина);
		ПараметрыЗадания.Вставить("ЭксклюзивноеВыполнение", Ложь);
		ПараметрыЗадания.Вставить("Параметры", ПараметрыЗапуска);
		// Если завершилось аварийно как часто повторять
		ПараметрыЗадания.Вставить("ИнтервалПовтораПриАварийномЗавершении", 60); 
		ПараметрыЗадания.Вставить("КоличествоПовторовПриАварийномЗавершении", 3); 
        	// Если завершилось аварийно сколько раз пытаться
		Расписание = Новый РасписаниеРегламентногоЗадания;      
		// Каждые три минуты каждый день (установите свое)
		Расписание.ПериодПовтораВТечениеДня   = 180;  
		Расписание.ПериодПовтораДней       = 7;    
		ПараметрыЗадания.Вставить("Расписание", Расписание);
		РегламентныеЗаданияСервер.ДобавитьЗадание(ПараметрыЗадания); 
		
	КонецЕсли;

КонецПроцедуры
```
```js
&НаКлиенте
Процедура СоздатьРегламентноеЗадание(Команда)
	СоздатьРегламентноеЗаданиеНаСервере();
КонецПроцедуры
```
`
the general module «ДополнительныеОтчетыИОбработки»:`
```js
&Перед("ВыполнитьОбработкуПоРегламентномуЗаданию")
Процедура тснВыполнитьОбработкуПоРегламентномуЗаданию(ВнешняяОбработка, ИдентификаторКоманды)
	Если ТипЗнч(ВнешняяОбработка) = Тип("Строка") И ВнешняяОбработка = "тснОтправкаНоменклатурыСайт" Тогда 
     		тснСайтОбщегоНазначения.ИнтеграцияНоменклатура_ВкусДоставка();	
	ИначеЕсли ТипЗнч(ВнешняяОбработка) = Тип("Строка") И ВнешняяОбработка = "тснПолучениеЗаказовСайт" Тогда 	
		тснСайтОбщегоНазначения.ИнтеграцияЗаказыКлиентов_ВкусДоставка();	
	КонецЕсли;
КонецПроцедуры
```
