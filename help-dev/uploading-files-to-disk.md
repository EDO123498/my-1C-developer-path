- [x] __Loading some files on disk__
```js
&НаКлиенте
Процедура тснСохранитьМакет(Команда)
	
	Если Элементы.тснМакеты.ТекущиеДанные.ФайлМакета.Пустая() Тогда
		Возврат;
	КонецЕсли;
	
	Проводник = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога); 
	Проводник.Выбрать();
	лПолученныйПуть = Проводник.Каталог;		
	
	Если лПолученныйПуть = "" Тогда 
		Возврат;
	КонецЕсли;
	
	лВыбранныеФайлы = Элементы.тснМакеты.ВыделенныеСтроки;
	
	Для Каждого Строка Из лВыбранныеФайлы Цикл
		
		ЭлементФормы = Объект.тснМакеты.Получить(Строка);
		ИсполнительныйФайлСсылка = ЭлементФормы.ФайлМакета;
		ИсполнительныйФайлНаименование = ЭлементФормы.ИмяМакета;
		
		лДвоичныеДанные = ПодготовитьФайлыНаСервере(ИсполнительныйФайлСсылка);
		лДвоичныеДанные.Записать(лПолученныйПуть + "\" + ИсполнительныйФайлНаименование);
		
	КонецЦикла;
	
КонецПроцедуры
```
- [x] __Loading some tabDOC on disk__
```js
&НаКлиенте
Процедура тснСохранитьТЗ(Команда)
		
	Проводник = Новый ДиалогВыбораФайла(РежимДиалогаВыбораФайла.ВыборКаталога); 
	Проводник.Выбрать();
	лПолученныйПуть = Проводник.Каталог;
	
	Если лПолученныйПуть = "" Тогда 
		Возврат;	
	КонецЕсли;
	
	лВыбранныеФайлы = Элементы.тснДеревоТЗ.ВыделенныеСтроки;
	
	Для Каждого Строка Из лВыбранныеФайлы Цикл
		
		ЭлементФормы = Объект.тснПараметрыТЗ.Получить(Строка);
		ИсполнительныйФайлНаименование = Элементы.тснДеревоТЗ.ДанныеСтроки(Строка).Параметр;
		
		лТабличныйДокумент = тснПечатьТЗНаСервере(Строка);
		лТабличныйДокумент.Записать(лПолученныйПуть + "\" + ИсполнительныйФайлНаименование 
		+ ".pdf", ТипФайлаТабличногоДокумента.PDF);	
		
	КонецЦикла;
	
КонецПроцедуры
```
```js
&НаСервере
Функция тснПечатьТЗНаСервере(пИдентификатор)
	
	лСтрока = тснДеревоТЗ.НайтиПоИдентификатору(пИдентификатор);
	
	лТабличныйДокумент = Новый ТабличныйДокумент;
	
	лМакет = Документы.ЗаказКлиента.ПолучитьМакет("тснБланкТЗ");
	
	лОбластьЗаголовка = лМакет.ПолучитьОбласть("Заголовок");
	лОбластьЗаголовка.Параметры.Организация = Объект.Организация;
	лОбластьЗаголовка.Параметры.Номенклатура = лСтрока.Параметр;
	
	лСтрокаНоменклатуры = тснПолучитьСтрокуТовараПоИдентификатору(лСтрока.УникальныйИдентификаторТовара);
	лОбластьЗаголовка.Параметры.ВидНоменклатуры = лСтрокаНоменклатуры.Номенклатура.ВидНоменклатуры;
	лОбластьЗаголовка.Параметры.Заказ = Объект.Ссылка;
	лОбластьЗаголовка.Параметры.Сумма = Объект.СуммаДокумента;
	лОбластьЗаголовка.Параметры.СОП = Объект.Менеджер;
	лТабличныйДокумент.Вывести(лОбластьЗаголовка);
	
	Если ЗначениеЗаполнено(Объект.Договор.Номер) Тогда
		лОбластьЗаголовка = лМакет.ПолучитьОбласть("НомерДоговора");
		лОбластьЗаголовка.Параметры.Номер = Объект.Договор.Номер;
		лТабличныйДокумент.Вывести(лОбластьЗаголовка);
	КонецЕсли;
	Если Лев(Объект.тснНомерДополнительногоСоглашения, 1) <> " " Тогда
		лОбластьЗаголовка = лМакет.ПолучитьОбласть("НомерДопСоглашения");
		лОбластьЗаголовка.Параметры.Номер = Объект.тснНомерДополнительногоСоглашения;
		лТабличныйДокумент.Вывести(лОбластьЗаголовка);
	КонецЕсли;
	лОбластьЗаголовка = лМакет.ПолучитьОбласть("НомерДоговораОферты");
	
	лТабличныйДокумент.Вывести(лОбластьЗаголовка);
	
	лОбластьЗаголовка = лМакет.ПолучитьОбласть("ПустаяСтрока");
	лТабличныйДокумент.Вывести(лОбластьЗаголовка);
	
	лОбластьЗаголовка = лМакет.ПолучитьОбласть("ШапкаТаблицы");
	лТабличныйДокумент.Вывести(лОбластьЗаголовка);
	
	лПараметрыТЗ = лСтрока.ПолучитьЭлементы();
	
	лОбластьСтроки = лМакет.ПолучитьОбласть("Строка");
	Для каждого лПараметр Из лПараметрыТЗ Цикл
		лОбластьСтроки.Параметры.Заполнить(лПараметр);
		лТабличныйДокумент.Вывести(лОбластьСтроки);
	КонецЦикла;
	Возврат лТабличныйДокумент;
	
КонецФункции
```
