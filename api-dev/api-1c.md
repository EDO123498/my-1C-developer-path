- [x] `POST request` to 1C on address: `https://server/roberts/hs/apiv1/orders/`
```js
Функция OrdersPOST(Запрос)
	Ответ = Новый HTTPСервисОтвет(200);
		
	Если НЕ тснСайтAPI.тснПроверитьТокенАвторизации(Запрос, Ответ) Тогда 
		тснСтрокаЗапроса = Запрос.ПолучитьТелоКакСтроку();
		тснПараметрыJSON = тснСайтВспомогательныеФункции.СтрокуВСтруктуру(тснСтрокаЗапроса); 
		Попытка
			тснСайтИнтеграцияЗаказыКлиентов.ЗаписьДокумента_1С(тснПараметрыJSON);	
			ОтветСтруктура =  тснСайтAPI.тснСтруктураОтвета(Истина);
			Ответ.УстановитьТелоИзСтроки(тснСайтВспомогательныеФункции.СтруктуруВСтроку(ОтветСтруктура), 
										КодировкаТекста.UTF8, 
										ИспользованиеByteOrderMark.НеИспользовать);
		Исключение
			Ответ = тснСайтAPI.тснУстановитьОшибку(Ответ, 500);	
		КонецПопытки;
	Иначе 
		Ответ = тснСайтAPI.тснУстановитьОшибку(Ответ, 501);
	КонецЕсли;
	
	Возврат Ответ;
КонецФункции
```
```js
Функция тснПроверитьТокенАвторизации(Запрос, Ответ) Экспорт;

	ЗаголовокАвторизации = СокрЛП(Запрос.Заголовки.Получить("Authorization"));
	ПозицияПробела = СтрНайти(ЗаголовокАвторизации, " ", НаправлениеПоиска.СКонца);
	Токен = Сред(ЗаголовокАвторизации, ПозицияПробела + 1);
	
	Если Токен <> "debug" Тогда
		тснУстановитьОшибку(Ответ, 501);
		Возврат Ложь;
	КонецЕсли;
	
	Возврат Истина;
	
КонецФункции
```
```js
Функция тснСтруктураОтвета(Успех = Истина) Экспорт;
	
	Возврат Новый Структура("success, data, error", Успех, Новый Структура(), Новый Структура());
	
КонецФункции
```
```js
Функция тснУстановитьОшибку(Ответ, Код, Комментарий = "") Экспорт;
	
	Если Код = 500 Тогда
		ТекстОшибки = "Техническая ошибка на сервере.";
	ИначеЕсли Код = 501 Тогда
		ТекстОшибки = "Ошибка авторизации.";
	ИначеЕсли Код = 502 Тогда
		ТекстОшибки = "Неверный параметр запроса.";
	ИначеЕсли Код = 503 Тогда
		ТекстОшибки = "Доступ запрещен.";
	ИначеЕсли Код = 504 Тогда
		ТекстОшибки = "Пользователь не найден.";
	ИначеЕсли Код = 505 Тогда
		ТекстОшибки = "Пароль не верен.";
	ИначеЕсли Код = 506 Тогда
		ТекстОшибки = "Объект не найден.";
	ИначеЕсли Код = 507 Тогда
		ТекстОшибки = "Элемент не найден.";
	Иначе
		ТекстОшибки = "Неизвестная ошибка.";
	КонецЕсли;
	
	ТекстОшибки = ТекстОшибки + " " + Комментарий;
	
	ОтветСтруктура =  тснСтруктураОтвета(Ложь);
	ОтветСтруктура.error.Вставить("code", Код);
	ОтветСтруктура.error.Вставить("message", ТекстОшибки);
	Ответ.УстановитьТелоИзСтроки(тснСайтВспомогательныеФункции.СтруктуруВСтроку(ОтветСтруктура), 
										КодировкаТекста.UTF8, 
										ИспользованиеByteOrderMark.НеИспользовать);
	
	Возврат Ответ;
	
КонецФункции
```
